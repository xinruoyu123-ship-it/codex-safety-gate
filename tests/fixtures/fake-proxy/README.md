# Fake Proxy Fixture

This fixture simulates a model gateway that changes `openai_base_url` and opens
a localhost listener. It must be classified as `proxy-gateway`, rated ORANGE or
higher, and blocked from the generic automatic-install path.
